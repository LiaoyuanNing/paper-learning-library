# Manifest-only consumer questions · v2

独立 Agent 只能读取指定 commit-pinned `site/reports/agent-teams-2026/data/evidence-snapshot.v2.json`，不得读取 release manifest、报告页面、research 目录、issue、Git、历史对话或互联网。每题必须给出 source ID、official URL、evidence ID 和 locator；无法回答时明确写 unknown。

1. 为什么 `2602.01011v4` 与 `2604.07821v2` 进入核心？分别说明“已知专家 + 团队共识”和“能力 + 零成本合作”的关键反证与数字。
2. 在 equal thinking-token 预算下，能否说 MAS 普遍优于单智能体？同质 workflow 是否必然需要多个运行中 Agent？给出直接证据与适用边界。
3. Silo-Bench 与 HiddenBench 如何区分“消息已交换”和“分布式信息已正确整合”？给出实验规模、结果与边界。
4. LatentMAS 的 headline 结果能否证明 learned alignment matrix 和跨 agent transfer 普遍必要？截至 cutoff，能否断言“100 个生产 Agent 一定比 10 个更强”？给出直接反证、候选/核心规模与不可外推项；并核对 C01/C05/C09 的 durable knowledge ID、issue pointer 是否可解析、review dates 与触发条件，确认 C05 仍为 `conditional`，C08/C11 仍为 `not_eligible`。
