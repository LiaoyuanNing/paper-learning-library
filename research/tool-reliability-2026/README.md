# Tool reliability research episode 002

AGE-396 的可复用研究资产。页面由唯一 `evidence-manifest.v1.json` 渲染；快照、逐篇 metadata audit、候选 ledger、查询、Critic、只读 consumer 与 browser QA 均在本目录。只保存元数据、短定位与综合判断，不保存论文正文或 PDF。

`age-396-v1` 是独立 immutable release，不修改 `age-174-v2` 的 payload、tag 或 digest。后续纠错必须发布新版本，并重新生成 snapshot digest。
